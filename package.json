{
  "name": "three-girls-library",
  "version": "1.0.0",
  "description": "Full-stack library management system - React + Spring Boot",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && gradlew.bat bootRun",
    "start": "npm run frontend",
    "help": "echo Run frontend: npm run frontend | Run backend: npm run backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
